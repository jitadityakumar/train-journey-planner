# Copied to upstream.env (gitignored) on jk-server-ccu by deploy.sh's first run —
# fill in and keep in sync manually after that (deploy.sh never overwrites an
# existing upstream.env). Tells poll_and_build.sh where to pull gtfs.zip from.
# The main app's own GET /api/gtfs/checksum and /api/gtfs/zip (GitHub issue #28)
# over plain HTTP on the tailnet — jkumar-server's Tailscale IP:port, matching
# how the app is reached everywhere else (see context.md), not MagicDNS.
UPSTREAM_URL=http://100.71.231.39:8010
