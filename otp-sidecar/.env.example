# No longer needed as of 2026-08-22 — docker-compose.prod.yml now binds
# 0.0.0.0:8080 (protected by jk-server-ccu's default-deny UFW policy, see its
# own comment) rather than a specific TAILSCALE_IP, to avoid a boot-order
# race with tailscaled. Kept only so a stale copied .env doesn't break
# anything; safe to delete.
